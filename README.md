Company Reviews Demo
====================

Sample application implementing user reviews (list, read, write) for companies.

## Development

Install dependencies run:

```bash
yarn
```

Start dev server:

```bash
yarn build:dev
```
